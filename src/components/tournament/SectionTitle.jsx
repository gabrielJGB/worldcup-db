import React from 'react'

const SectionTitle = ({title}) => {
  return (
    <div class={"font-semibold text-lg text-amber-400 text-shadow-xs text-shadow-black"}>{title}</div>
  )
}

export default SectionTitle